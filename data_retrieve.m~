data = csvread('att.csv');
timeStamp = 40;

desRoll = data(:,2);
roll = data(:,3);

desPitch = data(:,4);
pitch = data(:,5);

plot(desPitch);
xlabel('Timestamp')
ylabel('degrees')

hold on;

plot(pitch);

%%
input = desPitch(4780:7800);
output = pitch(4780:7800);

plot(input)
hold on;
plot(output)

axis tight

%% 
C
